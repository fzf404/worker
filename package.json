{
  "name": "worker",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint --cache --fix .",
    "deploy": "wrangler deploy",
    "format": "prettier --cache --write ."
  },
  "dependencies": {
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "@fzf404/eslint-config": "^0.5.7",
    "@fzf404/prettier-config": "^0.7.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.3",
    "wrangler": "^3.9.0"
  },
  "prettier": "@fzf404/prettier-config",
  "eslintConfig": {
    "extends": [
      "@fzf404/eslint-config"
    ]
  }
}
