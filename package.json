{
  "name": "worker",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "login": "wrangler login",
    "deploy": "wrangler deploy",
    "lint": "eslint --cache --fix .",
    "format": "prettier --cache --write ."
  },
  "dependencies": {
    "itty-router": "^4.2.2"
  },
  "devDependencies": {
    "@fzf404/eslint-config": "^0.5.12",
    "@fzf404/prettier-config": "^0.7.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "wrangler": "^3.37.0"
  },
  "prettier": "@fzf404/prettier-config",
  "eslintConfig": {
    "extends": [
      "@fzf404/eslint-config"
    ]
  }
}
