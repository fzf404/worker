{
  "name": "worker",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint --cache --fix .",
    "deploy": "wrangler deploy",
    "format": "prettier --cache --write ."
  },
  "dependencies": {
    "itty-router": "^4.0.22"
  },
  "devDependencies": {
    "@fzf404/eslint-config": "^0.4.6",
    "@fzf404/prettier-config": "^0.5.4",
    "eslint": "^8.47.0",
    "prettier": "^3.0.2",
    "wrangler": "^3.5.1"
  },
  "prettier": "@fzf404/prettier-config",
  "eslintConfig": {
    "extends": [
      "@fzf404/eslint-config"
    ]
  }
}
