{
  "name": "worker",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "login": "wrangler login",
    "lint": "eslint --cache --fix .",
    "format": "prettier --cache --write ."
  },
  "devDependencies": {
    "@fzf404/eslint-config": "^0.6.9",
    "@fzf404/prettier-config": "^0.7.5",
    "wrangler": "^3.75.0"
  },
  "prettier": "@fzf404/prettier-config",
  "eslintConfig": {
    "extends": [
      "@fzf404/eslint-config"
    ]
  }
}
